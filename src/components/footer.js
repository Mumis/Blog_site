import React from "react"
import { StaticQuery, graphql, Link } from "gatsby"
import { Wrapper } from "./footerStyle"


export default () => (
  <StaticQuery    
    render={data => (
      <footer>

      </footer>
    )}

  />
)